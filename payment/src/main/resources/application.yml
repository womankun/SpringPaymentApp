spring:
  datasource:
    url: jdbc:mysql://localhost:3306/payments?useSSL=false&serverTimezone=Asia/Tokyo
    username: user
    password: pass
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  type-aliases-package: com.example.payment.model
  mapper-locations: classpath:/mappers/*.xml
  configuration:
    map-underscore-to-camel-case: true

server:
  port: 55555
