<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.payment.mapper.PaymentMapper">

  <insert id="insertPayment" parameterType="com.example.payment.model.Payment">
    INSERT INTO payments (amount, status, card_number, card_expiry)
    VALUES (#{amount}, #{status}, #{cardNumber}, #{cardExpiry})
  </insert>

</mapper>
