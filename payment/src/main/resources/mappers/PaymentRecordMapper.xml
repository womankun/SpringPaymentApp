<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.payment.mapper.PaymentRecordMapper">

  <select id="selectAllRecords" parameterType="com.example.payment.model.Payment">
    SELECT * FROM payments ORDER BY created_at DESC
  </select>

  <select id="selectRecordByPaymentId" parameterType="com.example.payment.model.Payment">
    SELECT * FROM payments WHERE payment_id = #{paymentId}
  </select>

  <select id="selectRecordByDate" parameterType="com.example.payment.model.Payment">
    SELECT *
    FROM payments
    WHERE created_at &gt;= STR_TO_DATE(#{startDate}, '%Y-%m-%d')
      AND created_at &lt; DATE_ADD(STR_TO_DATE(#{endDate}, '%Y-%m-%d'), INTERVAL 1 DAY)
  </select>

  <select id="selectRecordByPaymentIdAndDate" parameterType="com.example.payment.model.Payment">
    SELECT *
    FROM payments
    WHERE payment_id = #{paymentId}
      AND created_at &gt;= STR_TO_DATE(#{startDate}, '%Y-%m-%d')
      AND created_at &lt; DATE_ADD(STR_TO_DATE(#{endDate}, '%Y-%m-%d'), INTERVAL 1 DAY)
  </select>

</mapper>
